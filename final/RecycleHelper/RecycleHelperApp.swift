import SwiftUI

/// Main app entry point for the Recycle Helper application
/// This is where the SwiftUI app lifecycle starts
@main
struct RecycleHelperApp: App {
    var body: some Scene {
        WindowGroup(content: {
            ContentView()
        })
    }
}
